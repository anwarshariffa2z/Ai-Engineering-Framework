orders-api

An HTTP API service for the ordering domain. Fixture subject for the AUD-0005
Backend Discovery reference producer.

Nothing here is executed by the audit. The manifest, route registrations,
middleware, handlers, clients, and schema are read as text at a fixed revision.
No endpoint is called, no message is enqueued, no database is contacted, and no
credential value appears anywhere in this tree.
